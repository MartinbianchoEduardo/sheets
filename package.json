{
  "name": "gastos",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy": "npm run deploy:master",
    "deploy:master": "npm run deploy:worker && npm run deploy:pages",
    "deploy:worker": "cd gastos-auth && npx wrangler deploy",
    "deploy:pages": "npm --prefix web run build && npx wrangler pages deploy ./web/dist --project-name=gastos --branch=main"
  }
}
