{
  "name": "gastos",
  "private": true,
  "scripts": {
    "deploy": "npm run deploy:master",
    "deploy:master": "npm --prefix web run build && npm run deploy:worker && npm run deploy:pages",
    "deploy:worker": "cd gastos-auth && npx wrangler deploy",
    "deploy:pages": "npx wrangler pages deploy ./web/dist --project-name=gastos-4fo --branch=main",
    "deploy:refactor": "npm --prefix web run build && npx wrangler pages deploy ./web/dist --project-name=gastos --branch=main",
    "deploy:worker:staging": "cd gastos-auth && npx wrangler deploy -c wrangler.staging.jsonc"
  }
}
